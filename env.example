# Copy this file to .env and update values. The script will read it automatically.
# Your Discord Application's Client ID (from https://discord.com/developers/applications)
DISCORD_CLIENT_ID=123456789012345678

# Comma-separated terms to mask from your Rich Presence (case-insensitive).
# Add your company/product names or sensitive words here.
MASK_TERMS=companyname,confidential,privacy,secret,budget,invoice,payroll,student,medical,legal,hr,finance,salary,contract,nda

# How often to push an update to Discord (seconds). 15 is a polite default.
UPDATE_EVERY_SECS=15

# How often the script checks for the active window (seconds). Keep small (>=1).
CHECK_INTERVAL=1
